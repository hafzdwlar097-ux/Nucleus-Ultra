## Packages
(none needed)

## Notes
PWA: assume backend serves /manifest.webmanifest and /sw.js; frontend registers service worker at runtime.
SEO: title/meta handled client-side via document.title + meta description update.
